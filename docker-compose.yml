services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: osama_portfolio_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - path: .env
        required: false
